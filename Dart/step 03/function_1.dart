void main(){
    //function may or not have parameter
    //depends with the nature of tasks that it performs

    beep(int milliseconds) {
    // make a sound play for milliseconds of time
    print("$milliseconds");
    }

    beep(344);
}